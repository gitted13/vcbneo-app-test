# Copy file này thành .env và điền giá trị thực
# cp .env.example .env

PORT=8888
CORS_ORIGINS=["http://localhost","https://reconcile.foxai.com.vn"]

DB_NAME=vcbneo

# SA password của ims_sqlserver — dùng 1 lần để tạo database vcbneo và user vcbneo_app
SA_PASSWORD=

# Password cho app user vcbneo_app (tự tạo mới, đặt tuỳ ý)
DB_PASSWORD=App@VCBNeo2026!
