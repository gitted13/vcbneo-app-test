DEBUG=false
UPLOAD_DIR=uploads
MAX_UPLOAD_MB=200
CORS_ORIGINS=["http://localhost:5173"]

# DB connection — chỉ cần khi chạy backend standalone (không qua docker-compose)
# Khi chạy qua docker-compose, các biến này được inject từ .env gốc
DB_SERVER=localhost
DB_NAME=vcbneo
DB_USER=sa
DB_PASSWORD=VCBNeo@2026!
